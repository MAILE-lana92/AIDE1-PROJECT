apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aide-api.fullname" . }}-test-connection"
  labels:
    {{- include "aide-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aide-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
